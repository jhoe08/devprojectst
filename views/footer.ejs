    <%# include('scripts') %>
    
    <script>
      let table = $("#basic-datatables").DataTable({
        // "aoColumnDefs": [{
        //     "aTargets": [ 1 ],
        //     "mDataProp": function ( data, type, val ) {
        //       if(type === 'display') {
        //         let classification = ''
        //         return `<span class="badge badge-count">${data[1]}</span>`
        //       }
        //       return data;
        //     }
        // }],
        "order": [[0, 'desc']],
        responsive: true,
        initComplete: function () {
          this.api()
          .columns()
          .every(function () {
            var column = this;
            var select = $('<select class="form-select"><option value=""></option></select>')
              .appendTo($(column.footer()).empty())
              .on("change", function () {
                var val = $.fn.dataTable.util.escapeRegex($(this).val());

                column
                  .search(val ? "^" + val + "$" : "", true, false)
                  .draw();
              });

            column
              .data()
              .unique()
              .sort()
              .each(function (d, j) {
                select.append(
                  '<option value="' + d + '">' + d + "</option>"
                );
              });
          });
        },
        // columns: [
        // { data: "id", title: "ID" },
        // { data: "title", title: "Title" },
        // {
        //   data: "created",
        //   title: "Date",
        //   render: function (data, type, row) {
        //       if (type === "display") {
        //           var date = new Date(data);
        //           return date.toLocaleDateString("en-US");
        //       }
        //       return data;
        //   },
        // },]
        
      })

      // target: 5,
      // render: $.fn.dataTable.render.number(null, null, 0, '₱')

      let employees = $("#employees").DataTable({
        layout: {
          topStart: {
            buttons: ['colvis']
          }
        },  
        "order": [[0, 'asc']],
        columnDefs: [{ visible: false, targets: [6,7] }],
        responsive: true,
        initComplete: function () {
          this.api()
          .columns()
          .every(function () {
            var column = this;
            var select = $('<select class="form-select"><option value=""></option></select>')
              .appendTo($(column.footer()).empty())
              .on("change", function () {
                var val = $.fn.dataTable.util.escapeRegex($(this).val());

                column
                  .search(val ? "^" + val + "$" : "", true, false)
                  .draw();
              });

            column
              .data()
              .unique()
              .sort()
              .each(function (d, j) {
                select.append(
                  '<option value="' + d + '">' + d + "</option>"
                );
              });
          });
        },
      })

      employees

      document.querySelectorAll('.toggle-vis').forEach((el) => {
      // // el.prepend('<i class="fas fa-check-square"></i>')

      // let icon = document.createElement('i')
      // icon.classList.add('far')
      // icon.classList.add('fa-square')

      // el.prepend(icon)

      el.addEventListener('click', function (e) {
          // e.preventDefault();

          let columnIdx = e.target.getAttribute('data-column');
          let column = table.column(columnIdx);

          // Toggle the visibility
          column.visible(!column.visible());
        });
      });

      $("#lineChart").sparkline([102, 109, 120, 99, 110, 105, 115], {
        type: "line",
        height: "70",
        width: "100%",
        lineWidth: "2",
        lineColor: "#177dff",
        fillColor: "rgba(23, 125, 255, 0.14)",
      });

      $("#lineChart2").sparkline([99, 125, 122, 105, 110, 124, 115], {
        type: "line",
        height: "70",
        width: "100%",
        lineWidth: "2",
        lineColor: "#f3545d",
        fillColor: "rgba(243, 84, 93, .14)",
      });

      $("#lineChart3").sparkline([105, 103, 123, 100, 95, 105, 115], {
        type: "line",
        height: "70",
        width: "100%",
        lineWidth: "2",
        lineColor: "#ffa534",
        fillColor: "rgba(255, 165, 52, .14)",
      });

    </script>
  </body>
</html>
